import 'package:flutter/material.dart';

class LiveWidgetsMarker extends StatelessWidget {
  const LiveWidgetsMarker({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
