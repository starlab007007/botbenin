import 'package:flutter/material.dart';

class BotsScreenNative extends StatelessWidget {
  const BotsScreenNative({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Mes Bots')),
      );
}
